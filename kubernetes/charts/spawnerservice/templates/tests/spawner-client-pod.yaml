apiVersion: v1
kind: Pod
metadata:
  name: spawner-cli
  labels:
    name: spawner-cli
spec:
  containers:
  - image: spawnerservice:0.0.1
    imagePullPolicy: Never
    command: [ "/bin/sh", "-c", "--" ]
    args: [ "while true; do sleep 30; done;" ]
    name: spawner-cli