apiVersion: v1
kind: Pod
metadata:
  name: go-cli
  labels:
    name: go-cli
spec:
  containers:
  - image: spawnerservice:0.0.1
    imagePullPolicy: Never
    command: [ "/bin/sh", "-c", "--" ]
    args: [ "while true; do sleep 30; done;" ]
    name: go-cli