apiVersion: v1
kind: Service
metadata:
  name: spawnerservice-service
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: {{ .Values.port }}
  selector:
    name: spawnerservice-deployment
